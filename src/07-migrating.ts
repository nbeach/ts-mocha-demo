//These are both strings
import es6 from "./modules/es6-code";
import ts from "./modules/ts-code";

//It tries to infer the type of the value from the ES6 module from its initial value
//let a: number = es6;
let b: string = ts;
